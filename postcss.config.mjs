const config = {
  plugins: {
    "@tailwindcss/postcss": {
      theme: {
        extend: {
          fontFamily: {
            futura: ["Futura PT", "sans-serif"],
          },
        },
      },
    },
  },
};

export default config;
